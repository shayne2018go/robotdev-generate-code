import { Expression } from '../../../../types/expression';

export const expression_identifier1: Expression.Identifier = {
  _statement_: 'expression',
  _expression_: 'identifier',
  escapedText: 'identifierText',
};
